export const APP_CONFIG = {
  // The production URL used for sharing links, especially in Capacitor mobile apps
  // where window.location.origin might resolve to localhost.
  PROD_URL: "https://artesanias-guapinol.vercel.app",
};
