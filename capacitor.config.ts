import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.artesaniasguapinol.app',
  appName: 'Artesanias Guapinol',
  webDir: 'dist'
};

export default config;
